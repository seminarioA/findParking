# Copy this file to infra/.env (gitignored) and fill in real values before
# running `docker compose -f infra/docker-compose.yml up`.
POSTGRES_DB=auth_db
POSTGRES_USER=auth_user
POSTGRES_PASSWORD=change-me-generate-a-strong-secret
