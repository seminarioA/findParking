[tool.ruff]
line-length = 100
target-version = "py311"
exclude = ["ui/frontend", "node_modules"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
ignore = [
    "B008", # function-call-in-default-argument: FastAPI's Depends(...) relies on this pattern.
]
